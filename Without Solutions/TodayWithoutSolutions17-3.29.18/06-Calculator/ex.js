var inputString = parseFloat(process.argv[4]);

var a = parseFloat(process.argv[2]);
var c = parseFloat(process.argv[3]);
var b = parseFloat(process.argv[4]);


if (a === "add") {
    outputNum = parseFloat(num1) + parseFloat(num2);
  }
  
  else if (operand === "subtract") {
    outputNum = parseFloat(num1) - parseFloat(num2);
  }
  else {
  outputNum = "Not a recognized command";
}
